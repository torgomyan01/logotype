export enum AlertSiteTypes {
  error = 'error',
  warning = 'warning',
  info = 'info',
  success = 'success'
}

export enum UserOnlineStatus {
  online = 'online',
  offline = 'offline'
}

export enum MessageStatus {
  read = 'read',
  noRead = 'noRead'
}
